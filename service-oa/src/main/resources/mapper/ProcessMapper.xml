<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.process.mapper.ProcessMapper">

    <select id="selectPage" resultType="com.atguigu.vo.process.ProcessVo">
        select
            a.id,a.process_code,a.user_id,a.process_template_id,a.process_type_id,a.title,a.description,a.form_values,a.process_instance_id,a.current_auditor,a.status,a.create_time,a.update_time,
            b.name as processTemplateName,
            c.name as processTypeName,
            d.name
        from oa_process a
                 left join sys_user d on a.user_id = d.id
                 left join oa_process_template b on b.id = a.process_template_id
                 left join `guigu-oa`.oa_process_type c on a.process_type_id = c.id
        <where>
            <if test="processQueryVo.keyword != null and processQueryVo.keyword != '' ">
                and (a.process_code like CONCAT('%',#{processQueryVo.keyword},'%') or  a.title like CONCAT('%',#{processQueryVo.keyword},'%') or d.phone like CONCAT('%',#{processQueryVo.keyword},'%') or d.name like CONCAT('%',#{processQueryVo.keyword},'%'))
            </if>
            <if test="processQueryVo.userId != null and processQueryVo.userId != ''">
                and a.user_id = #{processQueryVo.userId}
            </if>
            <if test="processQueryVo.status != null and processQueryVo.status != ''">
                and a.status = #{processQueryVo.status}
            </if>
            <if test="processQueryVo.createTimeBegin != null and processQueryVo.createTimeBegin != ''">
                and a.create_time >= #{processQueryVo.createTimeBegin}
            </if>
            <if test="processQueryVo.createTimeEnd != null and processQueryVo.createTimeEnd != ''">
                and a.create_time &lt;= #{processQueryVo.createTimeEnd}
            </if>
        </where>
        order by id desc
    </select>
</mapper>
